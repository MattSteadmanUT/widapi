# National WID API - Breakpoint Handoff (2026-06-12)

## 1) Why this document exists
This is the current pause-point handoff for the National WID project.
It captures what is built, what is deployed, what was changed in the latest work session,
what has been validated, and exactly what to do first when work resumes.

---

## 2) Repository and environment snapshot

- Repository root: c:\Users\mattsteadman\source\repos\fed-national-wid
- AWS profile: GovDev
- Region: us-gov-west-1
- CloudFormation stack: dev-national-wid-api
- Latest deploy status: UPDATE_COMPLETE
- API URL: [REDACTED-LIVE-API-URL]
- Aurora endpoint: [REDACTED-LIVE-AURORA-ENDPOINT]
- Ingestion Lambda: dev-national-wid-ingestion

Deployment evidence (latest session):
- Build succeeded (`dotnet build -c Release`)
- Deploy succeeded (`just deploy dev` -> stack UPDATE_COMPLETE)

---

## 3) What is built (current architecture)

### API (NationalWid.Api)
- .NET 8 API running behind API Gateway HTTP API
- Cognito authorizer wired for protected routes
- Table-native controllers implemented:
  - /labor-force
  - /ces
  - /industry
  - /wages
  - /projections
  - /lookups/*
- /health is unauthenticated
- WID response envelope and pagination pattern are in place

### Ingestion Lambda (NationalWid.Ingestion)
- Idempotent migrations + schema verification flow
- Dataset runner with per-dataset safety boundary and ingestlog writes
- Datasets currently wired in orchestration:
  - LAUS
  - CES
  - QCEW
  - OES/OEWS
  - Projections

---

## 4) Major ingestion improvements completed

### 4.1 LAUS and CES expanded to include state-level coverage
- LAUS: state + national ingestion completed and validated
- CES: state + national ingestion completed and validated
- CES SM parsing issue (20-char series handling) was corrected

Validated counts from successful invokes in this workstream:
- LAUS: 26,554
- CES: 31,579

### 4.2 QCEW reworked from failing JSON route to working CSV slices
- Previous source path returned 404s
- QCEW now uses BLS CSV slices (industry 10), then filters to national/state records
- Upsert path was batched for better write performance

Validation evidence in logs:
- Repeated completion lines showing `QCEW: completed - 6480 rows upserted`

### 4.3 OES/OEWS switched away from invalid BLS timeseries IDs
- The BLS public timeseries IDs being used returned `Series does not exist`
- OES ingest was replaced with OEWS downloadable workbook ingestion:
  - oesmYYnat.zip
  - oesmYYst.zip
- Parsing maps `total` + `major` groups into `iowage`

Current status:
- New source + parser implemented and deployed
- Runtime still shows `OES: completed - 0 rows upserted` in recent overlapping runs
- Most likely remaining issue is field normalization from workbook values (leading-zero code normalization)

### 4.4 Projections ingestor added and wired
- New Projections ingestor implemented
- Uses ProjectionsCentral REST JSON endpoint
- Mapped to `projectionsmatrix`
- Added `Projections` field in `IngestSummary`

Important scope adjustment:
- Endpoint was narrowed to all-occupations (`.../all/00-0000`) to keep runtime bounded during stabilization
- Endpoint behavior verified independently (`rows=55`, `total_pages=1`)

Current status:
- New implementation deployed
- CloudWatch still shows high page fetch counts in some recent runs due overlapping in-flight invocations from prior attempts

---

## 5) Files changed in this session (key handoff set)

- src/NationalWid.Ingestion/Ingestors/BlsQcewIngestor.cs
  - Switched to CSV slice ingestion + state/national filtering + batch upsert
- src/NationalWid.Ingestion/Ingestors/BlsOesIngestor.cs
  - Replaced API-series approach with OEWS ZIP/XLSX ingestion
- src/NationalWid.Ingestion/Ingestors/BlsProjectionsIngestor.cs
  - Added new projections ingestion implementation
- src/NationalWid.Ingestion/Function.cs
  - Wired projections run + summary field
- src/NationalWid.Ingestion/Models/Rows.cs
  - Added ProjectionsMatrixRow
- src/NationalWid.Ingestion/NationalWid.Ingestion.csproj
  - Added ClosedXML dependency for workbook parsing

---

## 6) Current deployment/runtime state summary

### Confirmed good
- Stack deploys successfully
- LAUS and CES state-level ingestion are working
- QCEW now returns non-zero upsert counts in Lambda logs

### Not fully closed yet
- OES/OEWS currently upserting 0 in recent runs (despite new source implementation)
- Projections runs need a clean single invocation validation after old overlapping runs are drained
- Some synchronous invokes timed out client-side before payload write due long/overlapping executions

---

## 7) First steps when work resumes (exact sequence)

1. Run one clean async ingestion invoke (do not use sync first):
   - `aws lambda invoke --function-name dev-national-wid-ingestion --invocation-type Event --profile GovDev --region us-gov-west-1 --cli-binary-format raw-in-base64-out --payload '{}' out.json`

2. Tail logs and wait for a single complete run marker:
   - Look for one contiguous run ending with
     `Ingestion complete. LAUS=... CES=... QCEW=... OES=... Projections=... Errors=...`

3. Verify table-level row deltas in Aurora:
   - `industry` (QCEW)
   - `iowage` (OES)
   - `projectionsmatrix` (Projections)

4. If OES remains zero, fix normalization logic first:
   - Normalize workbook code fields before filtering (`NAICS`, `AREA`, `OCC_CODE`)
   - Specifically preserve/restore leading zeros when source cells are numeric-like

5. After ingestion is stable, run API smoke checks:
   - `/industry`
   - `/wages`
   - `/projections`

---

## 8) Useful operational commands

- Build:
  - `just build`
- Deploy:
  - `just deploy dev`
- Ingest (current script path):
  - `just ingest-dev`
- Tail logs:
  - `just tail-ingestion-dev`
  - or
  - `aws logs tail /aws/lambda/dev-national-wid-ingestion --profile GovDev --region us-gov-west-1 --since 10m --format short`

---

## 9) Practical restart prompt for next session

Use this prompt to resume quickly:

"Resume National WID ingestion stabilization from HANDOFF-2026-06-12.md. Start by running one clean async invoke of dev-national-wid-ingestion, confirm final completion counts in CloudWatch, then close remaining gaps for OES (currently zero rows) and Projections runtime verification. Do not change API auth model or infrastructure architecture; focus on ingestion correctness and evidence-backed validation."
